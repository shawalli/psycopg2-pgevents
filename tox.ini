[tox]
envlist = py37

[testenv]
deps = -r{toxinidir}/requirements.txt
       -r{toxinidir}/test-requirements.txt
commands = pytest --cov=sqlalchemy_pgevents tests --cov-report term-missing
usedevelop = true
